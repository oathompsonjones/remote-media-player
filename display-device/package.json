{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "lint": "pnpm --dir .. exec eslint --config display-device/eslint.config.mjs display-device",
        "start": "node dist/display.js"
    },
    "devDependencies": {
        "@types/node": "^22.0.0",
        "typescript": "^5.0.0"
    }
}